# Instalación en Linux - Página de venta

La página es estática.

## Abrir localmente

```bash
cd ~/PaginaVenta
xdg-open index.html
```

## Publicar

Subir `index.html`, `styles.css`, `script.js` y `assets/` a un hosting estático.
